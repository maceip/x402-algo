# A routing hop is NOT a payee — it needs no payout address. It only forwards
# opaque bytes to the expert.
EXPERT_URL=http://localhost:4031

# Optional identity / port
# HOP_ID=hop-local-dev
# PORT=4030
