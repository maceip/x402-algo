# Account that submits settlements and pays the ALGO network fee (the sponsor).
# Fund this account with TestNet ALGO.
AVM_MNEMONIC=

# How many queries the operator sponsors per self-custody user before they must
# pay from their own wallet. Default 5.
# SPONSORED_LIMIT=5

# Optional PORT (default 4022)
# PORT=4022
