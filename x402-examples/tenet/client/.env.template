# Custodial signing wallet operated by the tenet service on the asker's behalf.
AVM_MNEMONIC=your 25-word mnemonic here

# Routing hop to send the query through (its /v1/match and /v1/answer).
HOP_URL=http://localhost:4030

# Optional prompt to ask.
# PROMPT=In one sentence, name one Monet painting technique.
